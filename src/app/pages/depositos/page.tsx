import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function DepositosPage() {
  return (
    <PagePlaceholder
      title="Depósitos"
      description="ABM de depósitos, responsable asignado y totales de cantidad/costo calculados por trigger."
    />
  );
}
